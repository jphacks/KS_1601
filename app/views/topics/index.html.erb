<div class="row">
  <div class="col-sm-9">
    <%=render :template=>"selected_companies/new" %>
    <%= high_chart("sample", @graph) %>
  </div>

  <div class="col-sm-3">
    <h1>スレッド一覧</h1>
    <%=link_to "個人チャット",static_pages_skyway_path%>
    <%=render 'topics/search' %>
    <% @topics.each do |topic| %>
     <li><%=link_to topic.title,topic %></li>
    <% end %>
  </div>
</div>
